const Historial = ({classname = ''}) => {
    return (
        <div className={`grid place-items-center rounded-brand-2 bg-blue-300 ${classname}`}>
            Historial
        </div>
    );
}
export default Historial;